--- a/fpsgame/scoreboard.cpp
+++ b/fpsgame/scoreboard.cpp
@@ -14,6 +14,11 @@
     VARP(showconnecting, 0, 0, 1);
     VARP(hidefrags, 0, 1, 1);
     VARP(showdeaths, 0, 0, 1);
+    VARP(showkdr, 0, 1, 1);   // SwiftGibs: kill/death ratio (all players)
+    VARP(showacc, 0, 0, 1);   // SwiftGibs: accuracy % (local row; remote needs extinfo)
+    VARP(showdmg, 0, 0, 1);   // SwiftGibs: total damage (local row; remote needs extinfo)
+    VARP(showcaptures, 0, 1, 1);   // SwiftGibs: flag captures, shown in CTF modes (all players)
+    VARP(showstreakcol, 0, 0, 1);   // SwiftGibs: best killstreak this match (all players; off by default)
 
     static hashset<teaminfo> teaminfos;
 
@@ -258,6 +262,51 @@
                 g.poplist();
             }
 
+            if(showcaptures && m_ctf)
+            {
+                g.pushlist();
+                g.strut(6);
+                g.text("caps", fgcolor);
+                loopscoregroup(o, g.textf("%d", 0xFFFFDD, NULL, o->flags));
+                g.poplist();
+            }
+
+            if(showkdr)
+            {
+                g.pushlist();
+                g.strut(6);
+                g.text("kdr", fgcolor);
+                loopscoregroup(o, g.textf("%.2f", 0xFFFFDD, NULL, o->deaths ? float(o->frags)/o->deaths : float(o->frags)));
+                g.poplist();
+            }
+
+            if(showstreakcol)
+            {
+                g.pushlist();
+                g.strut(6);
+                g.text("streak", fgcolor);
+                loopscoregroup(o, g.textf("%d", 0xFFFFDD, NULL, o->bstreak));
+                g.poplist();
+            }
+
+            if(showacc)
+            {
+                g.pushlist();
+                g.strut(6);
+                g.text("acc", fgcolor);
+                loopscoregroup(o, g.textf("%d%%", 0xFFFFDD, NULL, (o->totaldamage*100)/max(o->totalshots, 1)));
+                g.poplist();
+            }
+
+            if(showdmg)
+            {
+                g.pushlist();
+                g.strut(6);
+                g.text("dmg", fgcolor);
+                loopscoregroup(o, g.textf("%d", 0xFFFFDD, NULL, o->totaldamage));
+                g.poplist();
+            }
+
             g.pushlist();
             g.text("name", fgcolor);
             g.strut(12);
--- a/fpsgame/game.h
+++ b/fpsgame/game.h
@@ -549,6 +549,7 @@
     int lastpickup, lastpickupmillis, lastbase, lastrepammo, flagpickup, tokens;
     vec lastcollect;
     int frags, flags, deaths, totaldamage, totalshots;
+    int cstreak, bstreak;   // SwiftGibs: current + best-this-match killstreak (scoreboard column)
     editinfo *edit;
     float deltayaw, deltapitch, deltaroll, newyaw, newpitch, newroll;
     int smoothmillis;
@@ -560,7 +561,7 @@
 
     vec muzzle;
 
-    fpsent() : weight(100), clientnum(-1), privilege(PRIV_NONE), lastupdate(0), plag(0), ping(0), lifesequence(0), respawned(-1), suicided(-1), lastpain(0), attacksound(-1), attackchan(-1), idlesound(-1), idlechan(-1), frags(0), flags(0), deaths(0), totaldamage(0), totalshots(0), edit(NULL), smoothmillis(-1), playermodel(-1), ai(NULL), ownernum(-1), muzzle(-1, -1, -1)
+    fpsent() : weight(100), clientnum(-1), privilege(PRIV_NONE), lastupdate(0), plag(0), ping(0), lifesequence(0), respawned(-1), suicided(-1), lastpain(0), attacksound(-1), attackchan(-1), idlesound(-1), idlechan(-1), frags(0), flags(0), deaths(0), totaldamage(0), totalshots(0), cstreak(0), bstreak(0), edit(NULL), smoothmillis(-1), playermodel(-1), ai(NULL), ownernum(-1), muzzle(-1, -1, -1)
     {
         name[0] = team[0] = info[0] = 0;
         respawn();
