--- a/engine/main.cpp
+++ b/engine/main.cpp
@@ -81,7 +81,7 @@
     exit(EXIT_FAILURE);
 }
 
-int curtime = 0, lastmillis = 1, elapsedtime = 0, totalmillis = 1;
+int curtime = 0, lastmillis = 1, elapsedtime = 0, totalmillis = 1, curframetime = 0;   // curframetime (patch 24 wave 2, decoupled frame drawing): scaled duration since the last actual RENDER, not the last tick - see the sgdecouplefps module comment near limitfps() below
 // SwiftGibs AFK-aware auto-respawn: wall-clock (totalmillis-based) timestamp of the last
 // REAL SDL input event that reached the client - keyboard (processkey(), engine/console.cpp)
 // or mouse movement (the SDL_MOUSEMOTION case in checkinput() below); mouse buttons/wheel
@@ -936,6 +936,7 @@
 
 void checkinput()
 {
+    if(benchinputgatearmed) game::benchinputtick();   // SwiftGibs paced benchmark mode (patch 24): perf-counter timestamp for the input-gap latency proxy - armed-only, one bool check otherwise
     if(interceptkeysym) clearinterceptkey();
     //int lasttype = 0, lastbut = 0;
     bool mousemoved = false;
@@ -1061,11 +1062,161 @@
 VAR(menufps, 0, 60, 1000);
 VARP(maxfps, 0, 200, 1000);
 
-void limitfps(int &millis, int curmillis)
-{
-    if(benchmeasuring) return;   // sgbench: uncapped while measuring (patch 22)
-    int limit = (mainmenu || minimized) && menufps ? (maxfps ? min(maxfps, menufps) : menufps) : maxfps;
+// SwiftGibs sub-millisecond precise frame limiter (patch 24, perf wave 2 component 2). DEFAULT
+// OFF - sgprecisefps 0 takes the untouched stock branch at the bottom of limitfps() below,
+// byte-for-byte the same code that has always run there; `sgprecisefps && !menugoverned` is the
+// only new decision point on that path.
+//
+// Why: the stock branch quantizes its wait to whole milliseconds. At a high cap (maxfps 999 -
+// a 1.001ms target period) the `1000%limit` error accumulator's job is to nudge the INTEGER
+// delay up by 1ms every so often so the AVERAGE period tracks the true fractional one, but any
+// SINGLE frame still either sleeps 0ms or 1ms - a guaranteed pacing beat baked into the wait
+// itself (crawl finding 37), not a rounding error that washes out.
+//
+// The precise path instead times the wait on SDL_GetPerformanceCounter() (sub-millisecond
+// resolution): SDL_Delay(max(0, floor(remaining_ms) - 1)) sleeps everything except roughly the
+// last millisecond (cheap - yields the CPU, no different from the stock branch's own SDL_Delay
+// call), then a tight SDL_GetPerformanceCounter() poll loop spins out the sub-millisecond
+// remainder exactly. The spin is bounded to that leftover fraction (worst case a touch over 1ms
+// if SDL_Delay overshoots its own OS-timer granularity) - one core briefly hot right at each
+// frame boundary, not a sustained busy-wait for the whole frame.
+//
+// Pacing target: an ABSOLUTE next-frame deadline (perf-counter ticks), not "now + period" -
+// every call advances it by exactly one period from ITS OWN previous value, so a frame that
+// lands a little late does not push the schedule later; the following wait is simply shorter by
+// that same amount, and error cannot accumulate frame over frame (this replaces the stock
+// fpserror accumulator entirely on this path - the perf counter itself now plays that role, at
+// far finer resolution than 1000%limit ever gave). The one exception: if a call ever finds itself
+// more than a full period past the deadline (a real stall - alt-tab, a hitch, a paused
+// debugger), the deadline resyncs to "now" instead of trying to catch up, so a stall costs
+// exactly one slow frame and never a burst of unpaced frames right after it.
+//
+// Menu/minimized frames (the `menugoverned` branch just below - menufps governs the cap, not
+// maxfps) ALWAYS take the stock whole-ms path regardless of sgprecisefps: nobody needs
+// sub-millisecond pacing looking at a paused menu or a minimized window, and it keeps the new
+// machinery scoped to the live-gameplay case it was built to fix.
+VARP(sgprecisefps, 0, 0, 1);
+
+// SwiftGibs decoupled frame drawing (patch 24, perf wave 2 component 3). DEFAULT OFF -
+// sgdecouplefps 0 never enters the branch added below; limitfps()'s caller (engine/main.cpp's
+// main loop) always sees shouldrender left at its own default of true, so every tick renders,
+// exactly as before this existed.
+//
+// Why: even with sgprecisefps's sub-millisecond pacing (above), a capped frame still means
+// SDL events, net servicing and game-time advance ALL wait behind the same sleep/spin as the
+// render itself (crawl finding 38) - the whole loop iterates once per rendered frame. At
+// maxfps 60 that is a real ~16.7ms ceiling on input and network responsiveness, not just on
+// what gets drawn.
+//
+// sgdecouplefps turns the cap from "sleep until the next frame" into "render or don't render
+// THIS iteration" - the loop keeps iterating at whatever rate it can manage, pumping input,
+// servicing the network, and advancing game time every single time (engine/main.cpp's loop
+// body - checkinput/menuprocess/tryedit/game::updateworld/checksleep/serverslice/
+// recomputecamera/updateparticles/updatesounds all stay OUTSIDE any render gate, unchanged from
+// today); only the actual draw - fps history, gl_drawframe/gl_drawmainmenu, swapbuffers, and the
+// renderedframe bookkeeping - is skipped on an iteration that isn't due to render yet, the same
+// way the engine already skips that exact block today whenever `minimized` is true. A
+// non-render iteration parks itself briefly rather than spinning a core at 100%: SDL_Delay(1)
+// while more than about a millisecond from the render deadline, spinning only inside that final
+// millisecond (reusing sgprecisefps's own deadline/spin machinery above) so the eventual render
+// still lands precisely when sgprecisefps is also on.
+//
+// The 2x2 interaction matrix (this is the actual spec for how the two vars combine - not just a
+// description):
+//
+//   sgdecouplefps=0, sgprecisefps=0 (both off) - vendor behavior, byte-for-byte: the stock
+//     whole-ms sleep-the-whole-loop branch at the bottom of limitfps() runs, every tick renders.
+//   sgdecouplefps=0, sgprecisefps=1 - commit A alone: the precise coarse-delay+spin branch
+//     above runs every call, still sleeping/spinning the WHOLE loop each frame - no decoupling,
+//     just a tighter wait. Every tick still renders.
+//   sgdecouplefps=1, sgprecisefps=0 - the cap becomes a render/no-render decision; non-render
+//     iterations use a plain SDL_Delay(1) with no spin at all (acceptable per the design spec -
+//     input/net still get serviced roughly every millisecond, and the eventual render is only
+//     as precise as a millisecond-granularity SDL_Delay(1) makes it, same coarseness class as
+//     the stock branch's own single-wait precision).
+//   sgdecouplefps=1, sgprecisefps=1 - both: non-render iterations still use the cheap
+//     SDL_Delay(1) while far from the deadline, but the FINAL sub-millisecond approach to each
+//     render's deadline spins exactly like commit A instead of overshooting by a whole
+//     SDL_Delay(1) - decoupled input/net AND a precisely-timed render. This is the cell the
+//     round's own soak test exercises (maxfps 60, both vars 1).
+//
+// Menu/minimized frames (menufps-governed) and an active clip export capture
+// (game::clipexportcapturing()) both fall through to the non-decoupled branches above instead:
+// menus/minimized because there is no live match to keep input/net responsive for, and export
+// capture because engine/movie.cpp's deterministicframestep() has ALREADY paced this exact
+// iteration to land on one of ITS OWN output-frame boundaries (with its own SDL_Delay, before
+// limitfps() ever runs) - decouple skipping that render would silently drop an export frame the
+// export pacer already committed to. Both exclusions mean sgdecouplefps behaves exactly like
+// sgdecouplefps 0 in those two situations, never a third, undocumented behavior.
+VARP(sgdecouplefps, 0, 0, 1);
+
+void limitfps(int &millis, int curmillis, bool &shouldrender)
+{
+    if(benchmeasuring && !benchcapactive) return;   // sgbench: uncapped while measuring (patch 22); a PACED run (patch 24) sets benchcapactive so the cap stays active instead of being bypassed
+    bool menugoverned = (mainmenu || minimized) && menufps;   // SwiftGibs precise limiter (patch 24): named so both the cap computation below and the sgprecisefps gate agree on the same condition
+    int limit = menugoverned ? (maxfps ? min(maxfps, menufps) : menufps) : maxfps;
     if(!limit) return;
+
+    if(sgdecouplefps && !menugoverned && !game::clipexportcapturing())
+    {
+        // SwiftGibs decoupled frame drawing (patch 24): see the module comment above for the
+        // full design, including the 2x2 matrix. Separate deadline state from the
+        // sgprecisefps-only branch below - the two are mutually exclusive per call (this branch
+        // always returns), so there is no double-bookkeeping, only two independent schedules
+        // that pick up whichever one applies as the vars are (rarely) toggled at runtime.
+        static Uint64 deadline = 0;
+        static const Uint64 freq = SDL_GetPerformanceFrequency();
+        Uint64 periodticks = (Uint64)(freq / (double)limit);
+        Uint64 now = SDL_GetPerformanceCounter();
+        if(!deadline || now > deadline + periodticks) deadline = now;   // never armed, or stalled a full period behind: resync, don't burst-catch-up
+
+        if(now < deadline)
+        {
+            double remainingms = double(deadline - now) * 1000.0 / double(freq);
+            if(!(sgprecisefps && remainingms <= 1.0))
+            {
+                // far from the deadline, or sgprecisefps is off: a short, cheap tick - matrix
+                // cells (1,0) and the "far" part of (1,1) above. Input/net get serviced again
+                // almost immediately rather than waiting out the whole render period.
+                SDL_Delay(1);
+                shouldrender = false;
+                millis = getclockmillis();
+                return;
+            }
+            // final sub-millisecond stretch with sgprecisefps on - matrix cell (1,1): spin it
+            // out precisely instead of parking one more non-render iteration for a remainder
+            // smaller than SDL_Delay(1) can usefully wait out.
+            do { now = SDL_GetPerformanceCounter(); } while(now < deadline);
+        }
+        shouldrender = true;
+        deadline += periodticks;   // advance from the deadline's OWN previous value - see the sgprecisefps module comment above for why
+        millis = getclockmillis();
+        return;
+    }
+
+    if(sgprecisefps && !menugoverned)
+    {
+        // SwiftGibs precise limiter (patch 24): see the module comment above for the full
+        // design. static state persists across calls (and across sgprecisefps being toggled
+        // off and back on - the resync-on-stall check above self-heals a stale deadline the
+        // first time this path runs again).
+        static Uint64 precisedeadline = 0;   // absolute perf-counter tick this frame is due
+        static const Uint64 freq = SDL_GetPerformanceFrequency();
+        Uint64 periodticks = (Uint64)(freq / (double)limit);
+        Uint64 now = SDL_GetPerformanceCounter();
+        if(!precisedeadline || now > precisedeadline + periodticks) precisedeadline = now;   // never armed, or stalled a full period behind: resync, don't burst-catch-up
+        if(now < precisedeadline)
+        {
+            double remainingms = double(precisedeadline - now) * 1000.0 / double(freq);
+            int coarse = (int)remainingms - 1;   // floor(remaining_ms) - 1: leave the last ~1ms (plus fraction) to the spin below
+            if(coarse > 0) SDL_Delay((Uint32)coarse);
+            do { now = SDL_GetPerformanceCounter(); } while(now < precisedeadline);
+        }
+        precisedeadline += periodticks;   // advance from the deadline's OWN previous value, never from "now" - see the module comment above
+        millis = getclockmillis();        // refresh to the actual post-wait instant; unlike the stock branch's SDL_Delay(delay); millis += delay, the coarse-delay-plus-spin time isn't known in advance
+        return;
+    }
+
     static int fpserror = 0;
     int delay = 1000/limit - (millis-curmillis);
     if(delay < 0) fpserror = 0;
@@ -1393,8 +1544,9 @@
     for(;;)
     {
         static int frames = 0;
+        static int lastdrawmillis = 0;   // SwiftGibs decoupled frame drawing (patch 24): millis (getclockmillis() domain) at the last actual render - see the sgdecouplefps module comment near limitfps() above
         int millis = getclockmillis();
-        game::benchframe();          // sgbench: per-frame perf-counter capture (no-op unless armed, patch 22)
+        game::benchframe();          // sgbench: per-frame perf-counter capture (no-op unless armed, patch 22); as of patch 24 commit B this is the PENDING/MEASURING state machine only - the frame-time SAMPLE is taken by game::benchcaptureframe() below, inside the render-gated block, so sgbenchpaced measures render pacing rather than tick pacing once sgdecouplefps starts skipping renders (see that function's comment, fpsgame/client.cpp)
         game::drainclipresults();    // SwiftGibs clip writer thread (Task 2): drains completion/failure
                                       // messages from the background clip/recording writer once per
                                       // frame (no-op unless the writer thread has ever been started)
@@ -1405,9 +1557,14 @@
         // happened to be. Returns 0, and does nothing at all, in every other situation. limitfps
         // still runs afterwards and is a no-op at any maxfps at or above the export's frame rate
         // (the delay it computes is already used up); a maxfps BELOW it still wins, and costs the
-        // export duplicated frames exactly as it did before this existed.
+        // export duplicated frames exactly as it did before this existed. sgdecouplefps's own
+        // render/no-render decision (below) never fights this: limitfps() itself falls through to
+        // the non-decoupled branches whenever game::clipexportcapturing() is true, so an export
+        // in progress always renders every iteration exactly as it always has (see the
+        // sgdecouplefps module comment near limitfps() above).
         int exportframestep = recorder::deterministicframestep(millis);
-        limitfps(millis, totalmillis);
+        bool shouldrender = true;   // SwiftGibs decoupled frame drawing (patch 24): limitfps() only ever sets this false, and only when sgdecouplefps decides this iteration should not render - every other case (var off, menu/minimized, export capturing, no cap) leaves it at this default
+        limitfps(millis, totalmillis, shouldrender);
         elapsedtime = millis - totalmillis;
         static int timeerr = 0;
         int scaledtime = game::scaletime(elapsedtime) + timeerr;
@@ -1419,7 +1576,13 @@
 		lastmillis += curtime;
         totalmillis = millis;
         updatetime();
- 
+
+        // SwiftGibs decoupled frame drawing (patch 24): everything from here down to the render
+        // gate below runs EVERY iteration regardless of shouldrender - input pump, menu/edit
+        // input, world simulation (including c2sinfo()'s own send and gets2c()'s receive, both
+        // inside game::updateworld() - fpsgame/fps.cpp), sleep-command timers and the
+        // server/network slice. None of this is presentation, so decoupling the render cap from
+        // it is the entire point (crawl finding 38) - unchanged from the vendor loop either way.
         checkinput();
         menuprocess();
         tryedit();
@@ -1430,22 +1593,46 @@
 
         serverslice(false, 0);
 
-        if(frames) updatefpshistory(elapsedtime);
-        if(frames) game::fpsstatsframe(elapsedtime);   // SwiftGibs per-match fps stats: same frame time, no second clock
-        frames++;
-
-        // miscellaneous general game effects
+        // miscellaneous general game effects: also state advance, not presentation (particle
+        // simulation and the sound mixer's own channel sync both want to run every tick for
+        // smoothness regardless of render rate; recomputecamera() just refreshes camera state
+        // that rendering below will read if and when it happens) - all three stay outside the
+        // render gate, unchanged from the vendor loop.
         recomputecamera();
         updateparticles();
         updatesounds();
 
         if(minimized) continue;
 
+        if(!shouldrender) continue;   // SwiftGibs decoupled frame drawing (patch 24): non-render iteration - everything above already ran; only the presentation block below is skipped
+
+        // SwiftGibs decoupled frame drawing (patch 24): frametime/curframetime measure time
+        // since the last RENDER (getclockmillis() domain), not since the last tick - elapsedtime/
+        // curtime above stay tick-based for game simulation, which is correct even when a render
+        // and a tick are no longer the same event. Equals elapsedtime/curtime whenever every tick
+        // renders (sgdecouplefps 0, or no cap active). See the extern's comment (shared/iengine.h)
+        // for the one existing consumer this fixes (engine/rendergl.cpp's damage compass fade).
+        int frametime = millis - lastdrawmillis;
+        static int frametimeerr = 0;
+        int scaledframetime = game::scaletime(frametime) + frametimeerr;
+        curframetime = scaledframetime/100;
+        frametimeerr = scaledframetime%100;
+
+        // updatefpshistory/fpsstatsframe are presentation metrics (the HUD fps counter and the
+        // per-match fps-stats feature respectively) - both belong on render cadence, using
+        // frametime, not tick cadence using elapsedtime, or they would report the tick rate
+        // instead of the render rate whenever sgdecouplefps is skipping renders. frames/
+        // lastdrawmillis are therefore also render counters now, not tick counters.
+        if(frames) { updatefpshistory(frametime); game::fpsstatsframe(frametime); }   // SwiftGibs per-match fps stats: same frame time, no second clock
+        frames++;
+        lastdrawmillis = millis;
+
         inbetweenframes = false;
         if(mainmenu) gl_drawmainmenu();
         else gl_drawframe();
         swapbuffers();
         renderedframe = inbetweenframes = true;
+        game::benchcaptureframe();   // SwiftGibs paced benchmark mode (patch 24 commit B): this render's frame-time sample for sgbenchpaced - see that function's comment (fpsgame/client.cpp) for why it lives here and not in benchframe() above
     }
     
     ASSERT(0);   
--- a/engine/rendergl.cpp
+++ b/engine/rendergl.cpp
@@ -2025,7 +2025,13 @@
         gle::attrib(m.transform(vec2(0, 0)));
 
         // fade in log space so short blips don't disappear too quickly
-        scale -= float(curtime)/damagecompassfade;
+        // SwiftGibs decoupled frame drawing (patch 24 wave 2): curframetime, not curtime - this
+        // function only runs once per actual RENDER (gl_drawhud, below the render gate in
+        // engine/main.cpp's loop), so the fade must use time-since-last-render, not
+        // time-since-last-tick, or it would fade almost not at all whenever a render lands
+        // right after a short non-render tick (sgdecouplefps on). Identical to curtime whenever
+        // every tick renders.
+        scale -= float(curframetime)/damagecompassfade;
         damagedirs[i] = scale > 0 ? (pow(logscale, scale) - 1) / (logscale - 1) : 0;
     }
     if(dirs) gle::end();
--- a/fpsgame/client.cpp
+++ b/fpsgame/client.cpp
@@ -12,14 +12,23 @@
 #include <dirent.h>
 #endif
 
-// SwiftGibs benchmark mode (patch 22): two pieces of state that must live at TRUE global
-// scope, not inside namespace game (which this whole file lives inside from here down).
+// SwiftGibs benchmark mode (patch 22; extended by patch 24's capped-pacing mode): pieces of
+// state that must live at TRUE global scope, not inside namespace game (which this whole file
+// lives inside from here down).
 // - quit() is engine/main.cpp's plain global-scope exit function; forward-declared here so
 //   the unqualified quit() calls in the bench module below resolve to it instead of
 //   (re)declaring a separate, undefined game::quit.
 // - benchmeasuring is read by limitfps() (engine/main.cpp, also global scope) via the
 //   `extern bool benchmeasuring;` in shared/iengine.h; defining it inside namespace game
 //   would silently create a distinct game::benchmeasuring and fail to link.
+// - benchcapactive (patch 24) is also read by limitfps(): benchmeasuring alone always
+//   bypassed the cap outright (sgbench's whole point), but a PACED run must NOT bypass it -
+//   limitfps checks `benchmeasuring && !benchcapactive`, so a paced run's forced maxfps (set
+//   via setvar(), see sgbenchpaced below) actually governs the loop instead of being skipped.
+// - benchinputgatearmed (patch 24) is read by checkinput() (engine/main.cpp, also global
+//   scope): true only while a paced run's measured window is open, so checkinput() can
+//   timestamp its own entry for the input-gap latency proxy at a cost of one bool check on
+//   every call where a paced run isn't doing that (armed-only, per the design spec).
 // - minimized is engine/main.cpp's own global-scope flag (true while the window is
 //   minimized); benchframe() below polls it directly during MEASURING to poison, not
 //   silently record, a pass captured while minimized. Same file-scope-extern reasoning as
@@ -28,6 +37,8 @@
 extern void quit();
 extern bool minimized;
 bool benchmeasuring = false;
+bool benchcapactive = false;        // SwiftGibs paced benchmark mode (patch 24): see limitfps() in engine/main.cpp
+bool benchinputgatearmed = false;   // SwiftGibs paced benchmark mode (patch 24): see checkinput() in engine/main.cpp
 
 namespace game
 {
@@ -2999,6 +3010,19 @@
     static const double BENCHSTARTTIMEOUTMS = 10000.0;   // give up waiting for playback to start after this long
     VAR(benchdumpframes, 0, 1, 1);             // write benchframes-<row>.csv alongside the CSV row (not persisted)
 
+    // ---- SwiftGibs paced benchmark mode (patch 24): shared arm-time state ----
+    // Extends the classic sgbench state above rather than duplicating it - bencharmed,
+    // benchpendingticks, benchstartticks, BENCHCAP, benchframes_ms, benchcount,
+    // benchtruncated, benchlastticks, benchdemofile and BENCHSTARTTIMEOUTMS are ALL reused
+    // as-is by a paced run; only these are new.
+    static bool benchpaced = false;            // is the currently-armed run sgbenchpaced (vs classic sgbench)?
+    static int benchcapfps = 0;                // the requested cap - CSV capfps column + late15/late20 thresholds
+    static float *benchinputgaps_ms = NULL;    // allocated once on arm, paced runs only - preallocated like
+                                                // benchframes_ms above, same BENCHCAP capacity reasoning
+    static int benchinputgapcount = 0;
+    static Uint64 benchlastinputticks = 0;     // last checkinput()-entry perf-counter timestamp seen this run (0 = no sample yet)
+    static bool benchinputtruncated = false;   // mirrors benchtruncated above - set once benchinputgaps_ms fills (see benchinputtick() below)
+
     struct benchstats { float p50, p95, p99, onepctlow, worst; int stutters; };
 
     // p(k) on an already-sorted array: nearest-rank, truncating (not rounding) the index - the
@@ -3036,12 +3060,21 @@
         delete[] sorted;
     }
 
-    static void benchreport();   // forward decl - defined below; benchframe() below is the only caller
+    static void benchreport();        // forward decl - defined below; benchframe() below is the only caller
+    static void benchreportpaced();   // forward decl (patch 24) - defined below; benchframe() below is the only caller
 
     // Called unconditionally from the main loop (engine/main.cpp, right after
     // getclockmillis()) - a no-op before sgbench arms a run and forever after that run reports.
     // See the state-machine comment above the state block: this single function drives the
     // whole PENDING -> MEASURING -> report/error lifecycle by polling demoplayback directly.
+    //
+    // SwiftGibs decoupled frame drawing (patch 24 commit B): this function is the state machine
+    // ONLY as of this patch - it must still run every iteration regardless of sgdecouplefps
+    // (including the minimized-during-measurement poison check below, which has to fire even on
+    // an iteration that will never reach a render at all: main.cpp's own `if(minimized) continue;`
+    // skips straight past rendering, decouple or not). The actual per-frame TIME SAMPLE
+    // (benchframes_ms/benchlastticks) moved out to benchcaptureframe() below, called only from
+    // main.cpp's render-gated block - see that function's comment for why.
     void benchframe()
     {
         if(!bencharmed) return;
@@ -3059,6 +3092,8 @@
                 benchmeasuring = true;
                 benchstartticks = now;
                 benchlastticks = 0;
+                benchlastinputticks = 0;   // SwiftGibs paced benchmark mode (patch 24): fresh input-gap tracking for this run's window
+                benchinputgapcount = 0;
                 return;
             }
             double waitedms = double(now - benchpendingticks) * 1000.0 / double(freq);
@@ -3077,24 +3112,55 @@
         {
             bencharmed = false;
             benchmeasuring = false;
-            if(benchcount) benchreport();
+            benchcapactive = false;         // SwiftGibs paced benchmark mode (patch 24): resume normal limitfps semantics
+            benchinputgatearmed = false;    // SwiftGibs paced benchmark mode (patch 24): close the input-gap measured window
+            if(benchcount) { if(benchpaced) benchreportpaced(); else benchreport(); }
             else { conoutf("BENCH ERROR: no frames captured"); quit(); }
             return;
         }
 
         double sincestart = double(now - benchstartticks) * 1000.0 / double(freq);
+        if(benchpaced) benchinputgatearmed = sincestart >= 2000.0;   // SwiftGibs paced benchmark mode (patch 24): same 2s settle discard as the frame-capture window just below
+        // SwiftGibs decoupled frame drawing (patch 24 commit B): the frame-time SAMPLE that used
+        // to be taken right here now lives in benchcaptureframe() below (called only on a render,
+        // from main.cpp) - this check stays here, unconditional every tick, purely so the
+        // minimized-during-measurement poison still fires on an iteration that will never reach a
+        // render at all (see the module comment above benchframe()).
+        if(sincestart >= 2000.0 && benchlastticks && minimized)
+        {
+            // Poison the whole pass rather than silently recording a render-free,
+            // spun-free frame delta - see the "Minimized window" measurement rule above.
+            bencharmed = false;
+            benchmeasuring = false;
+            conoutf("BENCH ERROR: window minimized during measurement");
+            quit();
+            return;
+        }
+    }
+
+    // SwiftGibs decoupled frame drawing (patch 24 commit B): called from engine/main.cpp's
+    // render-gated block - only on an iteration that actually renders - NOT from benchframe()
+    // above, which runs every iteration whether or not it renders. This split is what makes
+    // sgbenchpaced measure RENDER pacing instead of TICK pacing once sgdecouplefps starts
+    // skipping renders: when decouple is off (or no cap is active) every tick renders, so this
+    // fires every tick and benchframes_ms is identical to the pre-patch-24-commit-B behavior;
+    // when decouple is on and skipping some renders, this only fires on the ticks that actually
+    // rendered, so benchframes_ms - and therefore the CSV's p50/p99/p999/late15/late20 - reflects
+    // true render cadence, which is the exact thing the design spec's decouple soak checks (a
+    // capfps 60 paced run should show p50ms ~= 16.7, not ~1, once sgdecouplefps is on). Guarded
+    // on benchmeasuring alone (not bencharmed): PENDING/timeout/error handling all stay in
+    // benchframe() above, this only ever needs to add a sample or start the running gap.
+    // checkinput()'s SEPARATE benchinputtick() (engine/main.cpp) is what still shows the full
+    // TICK rate via maxgap_input_ms - the two metrics are deliberately fed from different call
+    // sites for exactly this reason.
+    void benchcaptureframe()
+    {
+        if(!benchmeasuring) return;
+        Uint64 now = SDL_GetPerformanceCounter();
+        static const Uint64 freq = SDL_GetPerformanceFrequency();
+        double sincestart = double(now - benchstartticks) * 1000.0 / double(freq);
         if(sincestart >= 2000.0 && benchlastticks)
         {
-            if(minimized)
-            {
-                // Poison the whole pass rather than silently recording a render-free,
-                // spun-free frame delta - see the "Minimized window" measurement rule above.
-                bencharmed = false;
-                benchmeasuring = false;
-                conoutf("BENCH ERROR: window minimized during measurement");
-                quit();
-                return;
-            }
             double deltams = double(now - benchlastticks) * 1000.0 / double(freq);
             if(benchcount < BENCHCAP) benchframes_ms[benchcount++] = float(deltams);
             else benchtruncated = true;
@@ -3185,6 +3251,8 @@
         benchtruncated = false;
         benchlastticks = 0;
         benchmeasuring = false;
+        benchpaced = false;      // SwiftGibs paced benchmark mode (patch 24): classic sgbench is never the paced/capped mode
+        benchcapactive = false;
         copystring(benchdemofile, file);
         benchpendingticks = SDL_GetPerformanceCounter();
         bencharmed = true;
@@ -3208,6 +3276,246 @@
     });
     // ---- end SwiftGibs benchmark mode ----
 
+    // ---- SwiftGibs paced benchmark mode (patch 24): sgbenchpaced + pacing metrics + input-gap ----
+    //
+    // sgbenchpaced <demofile> <fps> is sgbench's capped sibling: it reuses the ENTIRE
+    // patch-22 state machine above (bencharmed/PENDING/MEASURING, the 10s start timeout, the
+    // minimized-window poison, the preallocated benchframes_ms capture array, quit-on-done) -
+    // nothing about IDLE -> PENDING -> MEASURING -> report/error is duplicated here. The only
+    // new arm-time state is benchpaced/benchcapfps (declared with the rest of the shared state
+    // above) plus the input-gap array below. What genuinely differs from sgbench:
+    // - limitfps (engine/main.cpp) must NOT be bypassed: benchmeasuring alone always bypassed
+    //   it (that is the entire point of uncapped sgbench). A paced run instead sets
+    //   benchcapactive (true global scope, like benchmeasuring itself) so limitfps's guard
+    //   becomes `benchmeasuring && !benchcapactive` - false for a paced run, so the cap keeps
+    //   governing the loop exactly as it would in normal play.
+    // - the cap itself is the real `maxfps` VARP, forced in memory for this run via
+    //   `setvar("maxfps", fps)` at arm time (same idiom already used elsewhere in this codebase
+    //   for programmatic var changes, e.g. engine/server.cpp's setvar("serveruprate", ...)) -
+    //   not a parallel, hand-rolled cap. It is never explicitly restored: sgbenchpaced always
+    //   quit()s the engine when it finishes (same as sgbench), and quit() writes maxfps's new
+    //   value into writecfg()'s target - but that target is the disposable bench-home directory
+    //   the runner recreates from scratch on every invocation (tools/bench/run-benchmark-paced.sh),
+    //   never the player's real config, so "not persisted" is true in every sense that matters.
+    // - a SEPARATE CSV, benchpacing.csv, with its own EXACT header (below) - the v1
+    //   benchresults.csv header contract that benchreport() writes above is untouched.
+    // - four new pacing metrics with no patch-22 equivalent: p999, jitter_stddev, late15/20
+    //   counts (see benchcomputepacedstats() below), and maxgap_input (see the input-gap
+    //   instrumentation further below).
+    //
+    // Pacing metrics (computed over the SAME measured-window frame array benchframe() already
+    // fills in above - this comment block is the spec, not just a description):
+    // - p50/p99/p999: reuse benchpercentile() (patch 22, above) UNCHANGED - the exact same
+    //   sorted[min(n-1,(size_t)(k/100.0*n))] convention, just at three k values. There is only
+    //   ever one percentile formula in this file.
+    // - jitter_stddev: POPULATION standard deviation of the frame times (divide by n, not
+    //   n-1) - see the hand-computed selftest comment below for the worked example.
+    // - late15_count / late20_count: target period = 1000.0/capfps ms; count frames STRICTLY
+    //   GREATER than 1.5x / 2.0x that period - same strict-inequality convention as patch 22's
+    //   "stutters" (> 2.0*median).
+    // - maxgap_input_ms: the largest checkinput()-entry-to-entry gap observed during the
+    //   measured window (see the input-gap instrumentation below); the CSV carries only the
+    //   max per the exact header contract, but the p99 gap is also computed (and printed to
+    //   the console, not the CSV) since Task 2/3 of this plan use it as their decouple-vs-
+    //   coupled latency proxy.
+    //
+    // Failure contract: IDENTICAL to sgbench above, reusing the exact same "BENCH ERROR: ..."
+    // messages from the shared state machine (timeout, no frames captured, minimized poison) -
+    // a paced run never gets its own distinct error-message vocabulary, and never writes a
+    // benchpacing.csv row on failure, matching "runners detect success by CSV row growth, not
+    // exit code" from the patch-22 measurement rules above.
+    //
+    // CSV header (written when the file is created, exact):
+    // utc,version,platform,demo,capfps,frames,seconds,avgfps,p50ms,p99ms,p999ms,jitter_stddev_ms,late15_count,late20_count,maxgap_input_ms
+    //
+    // ---- Input-gap instrumentation ----
+    // checkinput() (engine/main.cpp) times its own entry with SDL_GetPerformanceCounter(),
+    // gated by `if(benchinputgatearmed) game::benchinputtick();` - a single bool check on every
+    // call where no paced run's measured window is open (zero cost otherwise, per the design
+    // spec). benchinputgatearmed itself is maintained here, not in main.cpp: benchframe() sets
+    // it every MEASURING iteration to `sincestart >= 2000.0` - the identical 2s settle-discard
+    // condition the frame array uses - and clears it the moment the run ends. Because
+    // benchframe() runs BEFORE checkinput() in the main loop (engine/main.cpp: benchframe() is
+    // called right after getclockmillis(), checkinput() only after limitfps() and the
+    // time-advance block further down), the flag checkinput() reads this iteration was decided
+    // by THIS SAME iteration's benchframe() call, not a stale one-iteration-old value.
+    // benchinputtick() itself just records the perf-counter delta since the previous entry into
+    // a preallocated array, benchinputgaps_ms, sized BENCHCAP (1,000,000) - same capacity
+    // reasoning as benchframes_ms above, and same "stop recording rather than reallocate or
+    // crash if somehow exhausted" contract, just without a separate truncation flag (a
+    // multi-minute bench run at even a very high uncapped tick rate stays several orders of
+    // magnitude below 1,000,000 samples).
+    void benchinputtick()
+    {
+        Uint64 now = SDL_GetPerformanceCounter();
+        static const Uint64 freq = SDL_GetPerformanceFrequency();
+        if(benchlastinputticks)
+        {
+            double gapms = double(now - benchlastinputticks) * 1000.0 / double(freq);
+            if(benchinputgapcount < BENCHCAP) benchinputgaps_ms[benchinputgapcount++] = float(gapms);
+            else benchinputtruncated = true;   // mirrors benchframe()'s benchtruncated handling above
+        }
+        benchlastinputticks = now;
+    }
+
+    // Pacing-specific stats over a captured (or fixture) frame-time array - see the "Pacing
+    // metrics" comment above for the formulas this implements.
+    struct benchpacedstats { float p50, p99, p999, jitterstddev; int late15count, late20count; };
+
+    static void benchcomputepacedstats(const float *src, int n, int capfps, benchpacedstats &out)
+    {
+        float *sorted = new float[n];
+        memcpy(sorted, src, n*sizeof(float));
+        quicksort(sorted, n);
+
+        out.p50  = benchpercentile(sorted, n, 50.0);
+        out.p99  = benchpercentile(sorted, n, 99.0);
+        out.p999 = benchpercentile(sorted, n, 99.9);
+        delete[] sorted;
+
+        double sum = 0;
+        for(int i = 0; i < n; i++) sum += src[i];
+        double mean = sum / n;
+        double sqsum = 0;
+        for(int i = 0; i < n; i++) { double d = src[i] - mean; sqsum += d*d; }
+        out.jitterstddev = sqrtf(float(sqsum / n));
+
+        double period = 1000.0 / double(capfps);
+        int late15 = 0, late20 = 0;
+        for(int i = 0; i < n; i++)
+        {
+            if(src[i] > float(1.5*period)) late15++;
+            if(src[i] > float(2.0*period)) late20++;
+        }
+        out.late15count = late15;
+        out.late20count = late20;
+    }
+
+    // Input-gap array stats: max + p99 of the checkinput()-entry gaps (see the input-gap
+    // instrumentation comment above). Reuses the same quicksort + benchpercentile as every
+    // other stat in this file. n<=0 (no real measured window - e.g. sgbenchpacedselftest's
+    // fixture-only run below never opens one) reports 0.00 for both.
+    static void benchcomputeinputgapstats(const float *src, int n, float &maxgap, float &p99gap)
+    {
+        if(n <= 0) { maxgap = 0.0f; p99gap = 0.0f; return; }
+        float *sorted = new float[n];
+        memcpy(sorted, src, n*sizeof(float));
+        quicksort(sorted, n);
+        maxgap = sorted[n-1];
+        p99gap = benchpercentile(sorted, n, 99.0);
+        delete[] sorted;
+    }
+
+    // Only ever called with benchcount > 0 and benchpaced true (see benchframe() above) - the
+    // zero-frames case is handled entirely by the caller, so this never has to.
+    static void benchreportpaced()
+    {
+        benchpacedstats st;
+        benchcomputepacedstats(benchframes_ms, benchcount, benchcapfps, st);
+
+        float maxgapinput = 0.0f, p99gapinput = 0.0f;
+        benchcomputeinputgapstats(benchinputgaps_ms, benchinputgapcount, maxgapinput, p99gapinput);
+
+        double totalms = 0;
+        for(int i = 0; i < benchcount; i++) totalms += benchframes_ms[i];
+        double seconds = totalms/1000.0;
+        double avgfps = seconds > 0 ? benchcount/seconds : 0.0;
+
+        conoutf("BENCHPACED: demo=%s capfps=%d frames=%d seconds=%.2f avgfps=%.2f%s", benchdemofile, benchcapfps, benchcount, seconds, avgfps, benchtruncated ? " TRUNCATED" : "");
+        conoutf("BENCHPACED: p50=%.2fms p99=%.2fms p999=%.2fms jitter_stddev=%.2fms", st.p50, st.p99, st.p999, st.jitterstddev);
+        conoutf("BENCHPACED: late15_count=%d late20_count=%d", st.late15count, st.late20count);
+        conoutf("BENCHPACED: maxgap_input=%.2fms p99gap_input=%.2fms%s%s", maxgapinput, p99gapinput,
+                benchinputgapcount ? "" : " (no input samples)", benchinputtruncated ? " TRUNCATED" : "");
+
+        const char *fname = "benchpacing.csv";
+        bool existed = fileexists(findfile(fname, "r"), "r");   // same idiom as fpsstatswritelog() (fpsgame/fps.cpp) / benchreport() above
+        stream *o = openfile(fname, "a");
+        if(!o) { conoutf("BENCH ERROR: could not open %s for writing", fname); quit(); return; }
+        if(!existed) o->printf("utc,version,platform,demo,capfps,frames,seconds,avgfps,p50ms,p99ms,p999ms,jitter_stddev_ms,late15_count,late20_count,maxgap_input_ms\n");
+
+        time_t nowt = time(NULL);
+        string utcbuf;
+        strftime(utcbuf, sizeof(utcbuf), "%Y-%m-%dT%H:%M:%SZ", gmtime(&nowt));
+        const char *ver = getalias("swiftgibsversion");
+#ifdef WIN32
+        const char *plat = "windows";
+#elif defined(__APPLE__)
+        const char *plat = "mac";
+#else
+        const char *plat = "linux";
+#endif
+        o->printf("%s,%s,%s,%s,%d,%d,%.2f,%.2f,%.2f,%.2f,%.2f,%.2f,%d,%d,%.2f\n",
+                   utcbuf, ver, plat, benchdemofile, benchcapfps, benchcount, seconds, avgfps,
+                   st.p50, st.p99, st.p999, st.jitterstddev, st.late15count, st.late20count, maxgapinput);
+        delete o;
+
+        conoutf("BENCHPACED: done");
+        quit();
+    }
+
+    // Filename + fps supplied to sgbenchpaced, stashed at arm time. Order matters here exactly
+    // as it does for sgbench above: benchpaced/benchcapfps/benchcapactive and the forced
+    // setvar("maxfps", ...) are all set, and any leftover demo is stopped, BEFORE execute(cmd)
+    // starts loading the new one - same race-proofing reasoning as sgbench's own ordering
+    // comment above.
+    ICOMMAND(sgbenchpaced, "si", (char *file, int *fps),
+    {
+        if(bencharmed) { conoutf("BENCH ERROR: already benchmarking"); return; }
+        if(*fps <= 0) { conoutf("BENCH ERROR: sgbenchpaced requires fps > 0"); quit(); return; }
+        if(!fileexists(findfile(file, "r"), "r")) { conoutf("BENCH ERROR: demo file not found: %s", file); quit(); return; }
+        if(!benchframes_ms) benchframes_ms = new float[BENCHCAP];
+        if(!benchinputgaps_ms) benchinputgaps_ms = new float[BENCHCAP];
+        benchcount = 0;
+        benchtruncated = false;
+        benchlastticks = 0;
+        benchmeasuring = false;
+        benchpaced = true;
+        benchcapfps = *fps;
+        benchcapactive = true;         // limitfps (patch 24): do not bypass while this paced run is armed
+        benchinputgapcount = 0;
+        benchlastinputticks = 0;
+        benchinputtruncated = false;   // mirrors benchtruncated's reset above
+        benchinputgatearmed = false;   // becomes true once the measured window opens - see benchframe() above
+        copystring(benchdemofile, file);
+        setvar("maxfps", benchcapfps); // forced in memory for this run - see the module comment above for why this is never persisted in any sense that matters
+        benchcapfps = maxfps;          // review fix: setvar() clamps to maxfps's own VARP range (0..1000) - read back
+                                        // the ENFORCED value so the CSV capfps column and the late15/late20 thresholds
+                                        // below always reflect what actually ran, never a raw out-of-range request
+        benchpendingticks = SDL_GetPerformanceCounter();
+        bencharmed = true;
+        defformatstring(cmd, "stopdemo; mode -1; map \"%s\"", file);
+        execute(cmd);
+    });
+
+    // sgbenchpacedselftest hand-computed arithmetic (fixture REUSED from patch 22 above -
+    // benchselftestfixture = {10,10,10,10,10,10,10,10,30,10}, n=10: nine 10ms samples plus one
+    // 30ms sample; cap=100fps -> target period = 1000/100 = 10.00ms; sorted ascending:
+    // [10,10,10,10,10,10,10,10,10,30], 0-indexed):
+    //   p50  = sorted[min(9,(size_t)(50.0/100.0*10))]  = sorted[min(9,5)] = sorted[5] = 10.00
+    //   p99  = sorted[min(9,(size_t)(99.0/100.0*10))]  = sorted[min(9,9)] = sorted[9] = 30.00
+    //   p999 = sorted[min(9,(size_t)(99.9/100.0*10))]  = sorted[min(9,9)] = sorted[9] = 30.00
+    //     (99.9/100.0*10 = 9.99, truncated to size_t 9 - same truncating-not-rounding
+    //     convention as p50/p99 above)
+    //   late15_count: threshold = 1.5*10.00 = 15ms; only the 30ms sample exceeds it -> 1
+    //   late20_count: threshold = 2.0*10.00 = 20ms; only the 30ms sample exceeds it -> 1
+    //   jitter_stddev (population, divide by n not n-1):
+    //     mean = (9*10 + 30)/10 = 120/10 = 12.0
+    //     variance = (9*(10-12)^2 + (30-12)^2)/10 = (9*4 + 324)/10 = (36+324)/10 = 36.0
+    //     stddev = sqrt(36.0) = 6.00
+    //   maxgap_input: the selftest never opens a real measured window (a pure-fixture command
+    //   generates no checkinput() traffic of its own) - hardcoded 0.00, matching
+    //   benchcomputeinputgapstats()'s own n<=0 fallback above.
+    // Expected line:
+    // "BENCHPACED SELFTEST: n=10 capfps=100 p50=10.00 p99=30.00 p999=30.00 jitter_stddev=6.00 late15_count=1 late20_count=1 maxgap_input=0.00"
+    ICOMMAND(sgbenchpacedselftest, "", (),
+    {
+        benchpacedstats st;
+        benchcomputepacedstats(benchselftestfixture, 10, 100, st);
+        conoutf("BENCHPACED SELFTEST: n=10 capfps=100 p50=%.2f p99=%.2f p999=%.2f jitter_stddev=%.2f late15_count=%d late20_count=%d maxgap_input=0.00",
+                st.p50, st.p99, st.p999, st.jitterstddev, st.late15count, st.late20count);
+    });
+    // ---- end SwiftGibs paced benchmark mode ----
+
     // === SwiftGibs clip POV scan (QoL batch: export from another player's POV) ==============
     //
     // Bounded, read-only scan of a clip/recording's opening keyframe (writeclipwelcome(),
--- a/shared/iengine.h
+++ b/shared/iengine.h
@@ -4,6 +4,7 @@
 extern int lastmillis;                  // last time
 extern int elapsedtime;                 // elapsed frame time
 extern int totalmillis;                 // total elapsed time
+extern int curframetime;                // SwiftGibs decoupled frame drawing (patch 24 wave 2): scaled duration since the last actual RENDER (vs curtime's "since the last tick") - engine/rendergl.cpp uses this for per-render animation math that must not warp when a tick and a render stop being the same event; equals curtime whenever every tick renders (sgdecouplefps 0, or no cap active)
 extern int lastinputmillis;             // SwiftGibs AFK-aware auto-respawn: wall-clock (totalmillis-based) timestamp of the last real keyboard/mouse event (engine/main.cpp, engine/console.cpp) - declared here, not engine/engine.h, so unqualified lookup from inside namespace game (fpsgame/fps.cpp) finds this global, not a namespaced copy
 extern uint totalsecs;
 extern int gamespeed, paused;
@@ -582,6 +583,8 @@
 extern int bilinear;
 extern int clips;                                               // SwiftGibs instant replay on/off
 extern bool benchmeasuring;                                     // SwiftGibs benchmark mode (patch 22): limitfps() no-ops while this is set
+extern bool benchcapactive;                                     // SwiftGibs paced benchmark mode (patch 24): when set alongside benchmeasuring, limitfps() stays active (the cap governs the run)
+extern bool benchinputgatearmed;                                // SwiftGibs paced benchmark mode (patch 24): checkinput() timestamps its own entry while this is set
 
 // SwiftGibs preload pass (patch 23, slate C): sgpreloadstats (0/1, VAR) counts what got
 // warmed at map start (sgpreload_*) vs what still loaded lazily during play (sglazy_*),
--- a/shared/igame.h
+++ b/shared/igame.h
@@ -52,6 +52,8 @@
     extern void updateworld();
     extern void fpsstatsframe(int elapsed);  // SwiftGibs per-match fps stats: called once per frame with the frame time the engine already computed
     extern void benchframe();          // SwiftGibs benchmark mode (patch 22): per-frame perf-counter capture, no-op unless a run is armed
+    extern void benchinputtick();      // SwiftGibs paced benchmark mode (patch 24): perf-counter timestamp for a checkinput() entry, called only while benchinputgatearmed
+    extern void benchcaptureframe();   // SwiftGibs decoupled frame drawing (patch 24 commit B): the frame-time sample benchframe() used to take unconditionally - called only from engine/main.cpp's render-gated block now, so sgbenchpaced measures render pacing rather than tick pacing under sgdecouplefps
     extern void initclient();
     extern void physicstrigger(physent *d, bool local, int floorlevel, int waterlevel, int material = 0);
     extern void bounced(physent *d, const vec &surface);
